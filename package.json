{
  "name": "node-game-session",
  "description": "Session orchestrator for multiplayer games — lobbies, lifecycle, game server spawning, stats",
  "author": {
    "email": "guillaume.mathe@gmail.com",
    "name": "Guillaume MATHE"
  },
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/node-game-session.js",
      "default": "./dist/node-game-session.js"
    },
    "./src/*": "./src/*"
  },
  "module": "./dist/node-game-session.js",
  "types": "./dist/types/index.d.ts",
  "sideEffects": false,
  "files": [
    "dist/",
    "src/"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prebuild": "rm -rf dist",
    "build:bundle": "node build.js",
    "build:types": "tsc",
    "build": "node build.js && tsc",
    "prepublishOnly": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
